# 基于SL651协议的水文设备统一运维平台

## 产品设计及技术架构设计文档

## 1. 产品概述

### 1.1 背景与目标

随着水利信息化建设不断深入，水文监测系统面临设备厂家众多、协议不统一、运维成本高等问题。基于国家水利行业标准《水文监测数据通信规约（SL651-2014）》，设计开发一套统一的水文设备运维平台，旨在实现：

- **设备统一接入**：兼容各厂家遵循SL651协议的设备
- **远程智能运维**：支持远程配置、参数设置、故障诊断、固件升级
- **数据灵活转发**：支持向第三方平台实时转发数据
- **轻量化部署**：满足私有化部署需求，支持信创环境
- **多租户支持**：可为多个SaaS服务提供数据转发能力

### 1.2 设计原则

- **标准化**：严格遵循SL651-2014国家标准
- **轻量化**：资源占用低，部署简单，运维便捷
- **解耦设计**：接入服务与转发服务分离，可独立部署
- **信创兼容**：支持国产化CPU、操作系统、数据库
- **灵活扩展**：插件化架构，支持自定义功能扩展

### 1.3 用户角色

| 角色 | 职责 | 使用场景 |
|------|------|----------|
| 系统管理员 | 平台部署、租户管理、插件管理、系统监控 | 初始部署、扩容升级、故障排查 |
| 水文运维人员 | 设备管理、参数配置、远程控制、故障处理 | 日常巡检、参数调整、设备维护 |
| 数据分析师 | 数据查询、报表生成、趋势分析 | 数据统计分析、报告生成 |
| 第三方系统 | 数据接收、设备控制 | 省级平台对接、智慧水务集成 |

## 2. 业务需求与功能设计

### 2.1 核心业务需求

#### 设备接入管理

- 支持SL651协议的多种编码格式（ASCII/HEX/BCD）
- 支持TCP/TLS/串口等多种连接方式
- 设备自动发现与注册
- 设备心跳与状态监控

#### 远程运维操作

- 设备参数远程读取与修改
- 设备故障远程诊断

#### 数据采集与处理

- 实时数据采集与解析
- 数据质量校验与清洗
- 数据缓冲与补传
- 数据标准化输出

#### 数据转发与集成

- 多租户数据路由
- 多目的地数据转发
- 转发规则可配置
- 转发状态监控

### 2.2 功能模块设计

#### 2.2.1 设备接入模块

| 功能 | 描述 | SL651支持 |
|------|------|-----------|
| 设备发现 | 自动发现网络中的SL651设备 | 支持广播地址 |
| 设备注册 | 设备信息录入与认证 | 支持密码认证 |
| 协议解析 | SL651报文解析与校验 | ASCII/HEX/BCD编码 |
| 会话管理 | 设备连接状态维护 | 心跳保持、重连机制 |
| 安全认证 | 设备身份验证 | 密码/证书认证 |

#### 2.2.2 运维管理模块

| 功能 | 描述 | SL651指令 |
|------|------|-----------|
| 参数配置 | 读取/修改设备参数 | 40H-43H |
| 状态查询 | 查询设备工作状态 | 46H |
| 故障诊断 | 设备异常检测与定位 | 46H, 50H |

#### 2.2.3 数据管理模块

| 功能 | 描述 | 实现方式 |
|------|------|----------|
| 实时采集 | 定时/事件触发数据采集 | 定时报(32H)/加报(33H) |
| 数据缓冲 | 网络中断时数据暂存 | SQLite嵌入式数据库 |
| 数据补传 | 通信恢复后补传数据 | 自动补数机制 |
| 数据标准化 | 统一数据格式输出 | JSON/CSV格式 |

#### 2.2.4 转发服务模块

| 功能 | 描述 | 支持协议 |
|------|------|----------|
| 多租户管理 | 租户隔离与配置管理 | 配置文件 |
| 规则引擎 | 数据路由与转发规则 | 条件过滤、映射转换 |
| 多目的地转发 | 支持单条规则配置多个转发目标，并行转发 | MQTT, SQL, HTTP |
| 统一数据库目标 | 直接选择 MySQL/Postgres/SQLite，简化 DSN 配置 | MySQL, Postgres, SQLite |
| 转发日志审计 | 记录每次转发的状态、目标地址及完整报文内容 | 数据库存储 |
| 监控统计 | 转发状态监控与统计 | Prometheus指标 |

### 2.3 业务流程

#### 2.3.1 设备接入流程

```text
1. 设备连接 → 2. 协议握手 → 3. 身份认证 → 4. 参数同步
         ↓
5. 心跳维持 → 6. 数据上报 → 7. 命令响应 → 8. 异常处理
```

#### 2.3.2 数据转发流程

```text
1. 数据接收 → 2. 标准化处理 → 3. 规则匹配 → 4. 加入队列
         ↓
5. 插件处理 → 6. 协议转换 → 7. 目标发送 → 8. 状态记录与日志审计
         ↓
9. 成功确认 → 10. 失败重试 → 11. 最终处理
```

## 3. 系统架构设计

### 3.1 总体架构

```text
┌─────────────────────────────────────────────────────────────────────────┐
│                               水文设备运维平台                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────┐            MQTT            ┌─────────────────┐    │
│  │   接入服务         │◀────────────────────────▶│   转发服务         │    │
│  │  (Golang实现)      │                           │  (Node.js实现)    │    │
│  │                  │     控制消息+数据          │                  │    │
│  └─────────────────┘                           └─────────────────┘    │
│         │                                          │                   │
│         │                                          │                   │
│  ┌──────▼──────┐                          ┌───────▼───────┐           │
│  │  设备连接    │                          │  第三方平台     │           │
│  │ (SL651协议)  │                          │ (HTTP/MQTT/DB) │           │
│  └─────────────┘                          └───────────────┘           │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.2 技术架构分层

#### 3.2.1 接入服务架构（Golang实现）

```text
┌─────────────────────────────────────────────────────────────────────────┐
│                               接入服务                                    │
├───────────┬───────────┬───────────┬───────────┬───────────┬───────────┤
│   Web层   │ 业务逻辑层 │ 协议解析层 │  接入层    │  数据层    │  通信层    │
├───────────┼───────────┼───────────┼───────────┼───────────┼───────────┤
│ HTTP API  │设备管理服务│SL651解析器 │TCP/UDP    │SQLite缓冲 │ MQTT客户端 │
│ WebSocket │参数配置服务│协议路由器  │串口服务    │内存缓存    │ 控制通道    │
│ 控制台    │控制服务    │编解码器    │TLS安全    │数据持久化  │ 数据发布    │
└───────────┴───────────┴───────────┴───────────┴───────────┴───────────┘
```

#### 3.2.2 转发服务架构（Node.js实现）

```text
┌─────────────────────────────────────────────────────────────────────────┐
│                               转发服务                                    │
├───────────┬───────────┬───────────┬───────────┬───────────┬───────────┤
│  API层    │ 规则引擎层 │ 插件管理层 │ 通信层    │  数据层    │ 监控层     │
├───────────┼───────────┼───────────┼───────────┼───────────┼───────────┤
│ REST API  │规则解析器  │插件加载器  │MQTT订阅   │LevelDB    │ 指标收集   │
│ Web管理端 │条件评估器  │插件管理器  │WebSocket  │SQLite     │ 告警引擎   │
│ 配置接口  │路由决策器  │热更新机制  │HTTP推送   │内存缓存    │ 日志管理   │
└───────────┴───────────┴───────────┴───────────┴───────────┴───────────┘
```
